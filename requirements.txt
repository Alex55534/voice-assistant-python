# ===========================================================================
# requirements.txt для голосового помощника
# Установка: pip install -r requirements.txt
# ===========================================================================

# РАСПОЗНАВАНИЕ РЕЧИ
speechrecognition==3.10.0  # Основная библиотека для распознавания

# СИНТЕЗ РЕЧИ
gtts==2.4.0        # Онлайн синтез через Google (требует интернет)
pyttsx3==2.90      # Офлайн синтез (работает без интернета)

# АУДИО И ВОСПРОИЗВЕДЕНИЕ
pygame==2.5.2      # Воспроизведение аудио файлов
PyAudio==0.2.13    # Работа с микрофоном (установка зависит от ОС)

# СИСТЕМНЫЕ УТИЛИТЫ
psutil==5.9.6      # Управление процессами, закрытие приложений

# КОНФИГУРАЦИЯ
PyYAML==6.0.1      # Чтение YAML конфигов
python-dotenv==1.0.0  # Работа с .env файлами

# ДОПОЛНИТЕЛЬНО (необязательно)
asyncio==3.4.3     # Для асинхронной работы (улучшает отзывчивость)
colorama==0.4.6    # Цветной вывод в терминал

# ===========================================================================
# ВАЖНО: PyAudio может требовать дополнительной установки на Windows
# Инструкция: https://pypi.org/project/PyAudio/
# Альтернатива на Linux: sudo apt-get install python3-pyaudio
# ===========================================================================
