# =========================================================================
# requirements.txt для голосового помощника
# Установка: pip install -r requirements.txt
# =========================================================================

# ОСНОВНЫЕ БИБЛИОТЕКИ
# ===================

# 1. Распознавание речи через Google API
SpeechRecognition==3.10.0

# 2. Синтез речи (преобразование текста в речь)
gTTS==2.4.0

# 3. Воспроизведение аудио файлов
pygame==2.5.2

# 4. Работа с микрофоном (зависит от операционной системы)
#    На Windows может потребоваться установка дополнительных компонентов
#    См. инструкции в README.md
PyAudio==0.2.13

# =========================================================================
# ПРИМЕЧАНИЯ:
# - subprocess, threading, time, os, tempfile, webbrowser - встроены в Python
# - pyttsx3 и psutil были удалены из кода, поэтому не требуются
# =========================================================================
